CMAKE_MINIMUM_REQUIRED(VERSION 3.29)
PROJECT(DBDiffTool)

SET(CMAKE_CXX_STANDARD 20)

INCLUDE_DIRECTORIES(include)
AUX_SOURCE_DIRECTORY(source SOURCE_FILE)
ADD_EXECUTABLE(DBDiffTool main.cpp ${SOURCE_FILE})
